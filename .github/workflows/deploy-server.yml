name: Deploy Server Infrastructure

on:
  push:
    branches: [ "main" ]
    paths:
      - 'vgltu_launcher/server/**'
      - 'vgltu_launcher/admin-web/**'
      - 'vgltu_launcher/docker-compose.yml'
      - 'vgltu_launcher/nginx/**'

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Downcase IMAGE_NAME
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >>${GITHUB_ENV}
          echo "Lowercased IMAGE_NAME: ${GITHUB_REPOSITORY,,}"

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # --- СБОРКА BACKEND ---
      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./vgltu_launcher/server
          file: ./vgltu_launcher/server/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/backend:latest

      # --- СБОРКА ADMIN WEB ---
      - name: Build and push Admin Web
        uses: docker/build-push-action@v5
        with:
          context: ./vgltu_launcher/admin-web
          file: ./vgltu_launcher/admin-web/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/admin-web:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Переходим в папку проекта
            cd /home/root/VGLTU-Minecraft-launcher/vgltu_launcher/
            
            # Логинимся в реестр (чтобы скачать приватные образы)
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            # Обновляем код (docker-compose.yml, nginx conf)
            git pull origin main
            
            # Скачиваем свежие образы
            docker compose pull
            
            # Перезапускаем контейнеры (только те, что обновились)
            docker compose up -d
            
            # Чистим старые образы (чтобы диск не забился)
            docker image prune -f
            
            # Накатываем миграции БД (КРИТИЧНО ВАЖНО)
            docker compose exec -T backend alembic upgrade head